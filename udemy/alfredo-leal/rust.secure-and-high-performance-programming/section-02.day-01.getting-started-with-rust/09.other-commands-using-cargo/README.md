[ENGLISH]

==========

[SPANISH]
1) cargo build
   Va a generar archivo binario en una carpeta llamada "target".
2) cargo run
   Va a compilar.
3) cargo check
   Se asegura de que se va a ejecutar.
4) cargo build --release
   Es para realizar liberación del código, Rust genera comopilado mas optimizado en producción.